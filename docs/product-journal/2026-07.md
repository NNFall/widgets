# Журнал Kaigo — июль 2026

## 2026-07-18 — Зафиксирована центральная концепция Kaigo

Статус: продуктовая концепция

Что изменилось:

- Kaigo сформулирован не просто как генератор виджетов, а как способ создать
  индивидуального AI-сотрудника для сайта.
- Долгосрочная идея расширена до AI-слоя поверх сайта, который понимает
  контекст страницы и помогает посетителю в нужный момент.
- Позиционирование построено на трёх уровнях: простой вход в AI для бизнеса,
  персональный AI-сотрудник и будущий контекстный AI-слой.

Что увидит пользователь:

- Бизнес вставляет ссылку на сайт и получает AI-помощника в собственном стиле.
- Базовое обещание формулируется вокруг быстрого и понятного внедрения AI.

Доказательства:

- `docs/superpowers/specs/2026-07-18-kaigo-ai-employee-product-concept-design.md`

Ограничения:

- Это концепция и направление продукта, а не перечень уже выпущенных функций.

Потенциал для публикации:

- Отдельный пост-манифест о том, зачем бизнесу AI-сотрудник вместо типового
  чат-пузыря.

## 2026-07-23 — Выбран staged-подход к генерации

Статус: реализованный прототип

Что изменилось:

- Генерация разделена на последовательные видимые ревизии: арт-направление,
  основа, фирменный стиль, диалог и финальная анимационная отделка.
- Direct-подход через Gemini выбран основным направлением после сравнительных
  экспериментов с агентным вариантом.
- Генератор получил чатовый контракт: сообщения AI и посетителя, поле ввода,
  отправка и история должны оставаться понятными независимо от визуальной идеи.

Что увидит пользователь:

- Предпросмотр меняется поэтапно, а не появляется только в конце.
- Результат остаётся узнаваемым чатом, даже если внешний вид необычный.

Доказательства:

- `docs/superpowers/specs/2026-07-23-gemini-36-antigravity-comparison-design.md`
- `docs/evidence/raw-bureau-comparison-v1/`
- `docs/evidence/raw-bureau-abc/`

Ограничения:

- Эксперименты проводились на ограниченном числе сайтов.

Потенциал для публикации:

- Пост-сравнение «один запрос против поэтапной сборки».
- Пост с Direct/Antigravity вариантами и честным выбором архитектуры.

## 2026-07-25 — Визуальная проверка стала прозрачной и возобновляемой

Статус: развёрнуто и проверено

Что изменилось:

- Добавлены три специализированных визуальных критика и независимый судья.
- Замечания, исправления и пользовательские описания изменений выводятся в
  ленте генерации.
- Бюджеты браузерной проверки, технического ремонта и визуального ремонта
  разделены, чтобы одна повторяющаяся проблема не останавливала весь процесс.
- Последний валидный черновик остаётся доступным, даже если финальная проверка
  не завершилась.
- Запуск восстанавливается после обновления страницы.
- Статусы интерфейса переведены на русский язык.
- Искусственные ограничения выходных токенов Gemini удалены.

Что увидит пользователь:

- После перезагрузки страницы прогресс и текущий запуск не пропадают.
- Можно понять, что именно проверялось и почему потребовалось исправление.
- Даже незавершённая проверка не скрывает последний рабочий результат.

Доказательства:

- commit `76da191` — прозрачная и возобновляемая визуальная проверка;
- commit `29f7372` — удаление ограничений выходных токенов;
- commit `bda8c5c` — нейтральные к бренду проверки и русские статусы;
- `docs/superpowers/specs/2026-07-25-transparent-visual-review-and-resumable-builder-design.md`.

Ограничения:

- Ответы внешней модели могут завершаться не одновременно; система использует
  кворум, но должна показывать сокращённый состав проверки явно.

Потенциал для публикации:

- Отдельный технический пост «Почему AI нельзя просто поверить на слово».

## 2026-07-25 — Первый полностью проверенный прогон Flowwow

Статус: завершено, `quality_status=verified`

Что изменилось:

- По ссылке `https://about.flowwow.com/` и запросу «виджет в виде цветочка»
  собран AI-флорист Flora.
- Пройдены пять ревизий и финальная браузерная проверка на desktop и mobile.
- Проверены закрытое состояние, открытый чат и диалог после двух сообщений.
- Реальный чат сохранил историю между тремя последовательными обращениями.

Что увидит пользователь:

- Цветочный лаунчер, фирменная кремово-сливовая палитра, понятный чат,
  быстрые действия «Подобрать букет» и «Статус заказа».
- Ответы AI приходили примерно за 1,8–2 секунды.

Доказательства:

- run ID `7mA6R8Q3gS7JwPf98A5yqnQaIrlQXP_b`;
- 41 событие, 5 сохранённых ревизий и 6 браузерных снимков;
- длительность 872,17 секунды;
- суммарно 210 341 токен.

Ограничения:

- Анализ исходного сайта занял 8 минут 53 секунды и остаётся главным узким
  местом.
- Два критика завершили ответы, третий не завершил; судья принял кворум 2 из 3.
- Начальная высота открытого чата создаёт заметное пустое пространство при
  одном сообщении.

Потенциал для публикации:

- Главный пост текущего обновления: «Из ссылки на Flowwow — в AI-флориста».

## 2026-07-26 — Зафиксирована UX-модель публичного Kaigo Builder

Статус: продуктовый ресерч, реализация не начата

Что изменилось:

- Сравнены Google AI Studio Build, Lovable, Base44, Bolt, v0, Replit,
  Embeddable и классические widget-конструкторы.
- Рекомендуемый путь сформулирован как URL-first лендинг, сразу сохранённый
  проект, прозрачная фоновая генерация и единый workspace с диалогом и живым
  preview.
- Главное интерфейсное отличие Kaigo определено не как новый стиль карточек, а
  как показ AI-сотрудника прямо поверх реального сайта пользователя.
- Подготовлен отдельный промпт для генерации десяти визуальных концептов, не
  фиксирующий палитру и брендовый стиль раньше времени.
- После уточнения первого экрана создано пять визуальных референсов `16:9`:
  тёплый редакционный, строгий монохром, тёмный пространственный, графическая
  сетка и мягкий пространственный.
- В первой визуальной итерации из viewport были убраны hero-заголовок,
  пояснение, chips и демонстрация; главным объектом оставался центральный
  composer.
- Первая разреженная серия показала, что один composer делает экран слишком
  пустым. Решение пересмотрено: hero получает полноценный header, слоган,
  пояснение и визуальный proof, сохраняя composer главным действием.
- Создана вторая серия из десяти насыщенных `16:9` референсов с полностью
  разными цветовыми системами и композициями.
- Вторая серия показала обратную крайность: большие preview, карточки и
  плакатная типографика мешают мгновенно понять главное действие.
- Создана третья сбалансированная серия из десяти `16:9` референсов. Целевая
  плотность зафиксирована как один слоган, одна строка, один composer и одно
  компактное визуальное доказательство.
- После выбора светлого №1 и тёмного №3 создана четвёртая серия из восьми
  референсов. В ней три шага `ссылка → анализ → AI готов` соединены одним
  маршрутом, который приводит взгляд к полю URL.
- Для следующей итерации предпочтительны почти монохромная светлая либо
  премиальная тёмно-синяя палитра с одним акцентом.
- На основе внешнего референса с персонажем и карточками создана пятая серия из
  восьми narrative-концепций. Референс использован только для понимания энергии
  и композиции; робот, фирменный стиль и расположение элементов не копировались.
- В более насыщенном направлении правая половина становится цельной сценой:
  трансформация сайта, три портала, AI-ядро, лента либо генеративный объект.
  Карточки получили полноценные пояснения и разные формы.
- После пользовательского отбора уточнены три ветки: «до/после», B2B-лента и
  раскрывающийся генеративный объект. Создано восемь новых вариантов.
- Оффер переписан для бизнеса, а финальный результат везде назван AI-виджетом.
- Для тёмных экранов зафиксированы шесть палитр с HEX-кодами и ссылками на
  источники. Основным кандидатом стала понятная анимируемая трансформация сайта.
- После ревью тёмная насыщенная подача признана избыточной. Создано пять
  светлых концепций по два кадра: до сканирования и после появления виджета.
- Зафиксирован motion-инвариант: оффер и URL остаются неподвижны, меняется
  только правый демонстрационный блок. Выгоды проявляются вслед за сканером.
- Простые подписи заменены карточками с номером, иконкой, заголовком и
  объяснением. Виджет получил отдельную акцентную рамку и импульс.
- Созданы две перспективные пары, две панорамные пары и авторский раскрывающийся
  триптих. Всего — десять новых ключевых кадров.
- Пользователь выбрал `Perspective Orbit` как финальную основу первого экрана.
- Заголовок заменён на «Через 10 минут вы сможете сказать: наш бизнес использует
  AI», добавлен полноценный header.
- Созданы ещё семь последовательных секций: процесс, анализ, кейс, возможности,
  студия, помощь и финальный CTA.

Что увидит пользователь:

- Один главный ввод: URL сайта и необязательное пожелание.
- После запуска — понятные этапы и тот же проект, который постепенно становится
  редактором.
- В preview — исходный сайт с работающим виджетом, а не пустой canvas.

Доказательства:

- `docs/research/2026-07-26-ai-builder-ux-benchmark.md`;
- `docs/design-prompts/2026-07-26-kaigo-landing-reference-prompt.md`.
- `docs/research/assets/2026-07-26-kaigo-landing-references/`.
- `docs/research/assets/2026-07-26-kaigo-landing-references-rich/`.
- `docs/research/assets/2026-07-26-kaigo-landing-references-balanced/`.
- `docs/research/assets/2026-07-26-kaigo-landing-references-guided-flow/`.
- `docs/research/assets/2026-07-26-kaigo-landing-references-narrative/`.
- `docs/research/assets/2026-07-26-kaigo-landing-references-business-refined/`.
- `docs/research/assets/2026-07-26-kaigo-landing-animation-pairs-light/`.
- `docs/research/assets/2026-07-26-kaigo-landing-animation-cards-refined/`.
- `docs/research/assets/2026-07-27-kaigo-landing-full-sequence/`.

Ограничения:

- Это решение о пользовательском пути и композиции, а не утверждённый
  визуальный дизайн и не реализованное обновление интерфейса.

Потенциал для публикации:

- Пост «Почему Kaigo начинается с URL, а не с пустого промпта».
- Пост-сравнение: что Kaigo берёт у AI-конструкторов и от чего сознательно
  отказывается.

## 2026-07-26 — Подготовлен локальный мост Telegram ↔ Codex

Статус: реализовано и проверено локально; живой Telegram-бот ещё не настроен

Что изменилось:

- Появился отдельный Windows-процесс, который принимает разрешённые сообщения
  Telegram и продолжает выбранную задачу через `codex exec resume`.
- Добавлены белый список Telegram user ID, переключение между задачами,
  SQLite-очередь и защита от повторной обработки одного `update_id`.
- Очередь, привязки и уже созданные ответы восстанавливаются после перезапуска.
- Добавлены скрытая настройка токена, ручной запуск и автозапуск через Windows
  Task Scheduler без прав администратора.

Что увидит пользователь после локальной настройки:

- Сообщение из Telegram появляется новым ходом в той же истории Codex Desktop.
- Бот подтверждает место в очереди и возвращает финальный ответ Codex.
- Команда `/use <thread-id>` переключает Telegram-чат на другую задачу без
  изменения кода.

Доказательства:

- 33 изолированных теста моста;
- `ruff` и `compileall` проходят без ошибок;
- четыре PowerShell-скрипта проходят штатный синтаксический parser;
- ручной CLI-эксперимент ранее подтвердил, что `codex exec resume` добавляет
  ход в историю, видимую после перезапуска Codex Desktop;
- `docs/superpowers/specs/2026-07-26-windows-telegram-codex-bridge-design.md`.

Ограничения:

- Токен BotFather и Telegram ID ещё не введены, поэтому end-to-end проверка с
  реальным ботом не выполнена.
- Первая версия принимает только текст и не публикует сообщения в публичный
  канал.
- Одновременно выполняется только один ход Codex, остальные ждут в очереди.
- Telegram API не даёт ключ идемпотентности для `sendMessage`, поэтому жёсткий
  crash точно между отправкой фрагмента и записью cursor может повторить один
  фрагмент. Обычная ошибка доставки продолжает ответ без повтора подтверждённых
  частей.

Потенциал для публикации:

- После живого теста — пост «Теперь Telegram действительно продолжает мою
  задачу Codex, а не создаёт новый чат».

## 2026-07-27 — Новый сайт Kaigo и Studio развёрнуты на production

Статус: реализовано, проверено и опубликовано

Что изменилось:

- Утверждённая серия референсов стала живым адаптивным сайтом из восьми
  экранов на <https://kaigo.space/>.
- Hero повторяет `Perspective Orbit`: медленное сканирование макета сайта,
  уменьшение и смещение сцены, последовательное появление трёх карточек и
  акцент готового AI-виджета.
- Добавлены секции процесса, визуального анализа, кейса, возможностей, Studio,
  FAQ и финального CTA; переходы и появления поддерживают reduced motion.
- Реализована Studio с Builder API, живой лентой этапов, восстановлением
  активного запуска, desktop/mobile preview, отменой, повтором и доработкой.
- Статический frontend выпускается версионными релизами с атомарным
  переключением symlink. Маршруты старого приложения сохранены.

Проверка:

- 49 из 49 Vitest и 10 из 10 Playwright-сценариев прошли;
- TypeScript, ESLint, production build и `git diff --check` прошли;
- встроенный браузер подтвердил desktop/mobile, восемь секций и отсутствие
  горизонтального переполнения;
- production отдаёт главную с `200`, защищённые Studio/Builder с `401`, а
  прежние `/w/demka`, `/builder-demo/` и `/real-time/` продолжают отвечать `200`.

Доказательства:

- `docs/telegram/release-packets/2026-07-27-kaigo-marketing-site.md`;
- `docs/evidence/marketing-site/hero-desktop-final.png`;
- `docs/evidence/marketing-site/hero-mobile-final.png`;
- `docs/evidence/marketing-site/landing-sections-inapp-contact-sheet.jpg`;
- `docs/evidence/marketing-site/studio-desktop-current.png`.

Ограничения:

- Studio пока закрыта Builder Basic Auth;
- публикация виджета остаётся будущей функцией;
- для этого UI-релиза реальная платная генерация Gemini не запускалась.

Потенциал для публикации:

- Пост «Kaigo вышел из референса в живой сайт» с кадрами hero, секций и Studio.

## 2026-07-27 — Лендинг получил выразительную циклическую анимацию

Статус: опубликовано на production и проверено

Что изменилось:

- Первый экран теперь рассказывает историю целиком: исходный сайт медленно сканируется световой полосой с подписью «Сканирование…», три карточки последовательно входят с разными векторами, а увеличенный AI-виджет появляется с пружиной, импульсом и подписью.
- Первая cinematic-сборка намеренно длинная; после 12-секундной демонстрационной паузы запускаются более быстрые циклы с 11-секундным спокойным состоянием.
- В секциях процесса, анализа, кейса, возможностей, демонстрации Studio, FAQ и финального CTA добавлены собственные последовательности и микродвижения.
- Любая циклическая анимация останавливается вне viewport, при скрытой вкладке и при reduced motion. Возврат продолжает текущую фазу и CSS-кадр с сохранённого времени, а не создаёт второй таймер.
- Центральный чат получил отдельную оболочку Reveal, поэтому его фирменный поворот больше не перезаписывается входной scale-анимацией.
- Мобильная кнопка в демонстрации Studio увеличена до честной доступной области; контраст сообщения пользователя исправлен до WCAG AA.
- Настоящие Studio, Builder API, маршруты и авторизация не менялись.

Проверка:

- 92 из 92 Vitest-тестов прошли;
- 11 из 11 Playwright-сценариев прошли на desktop, mobile, reduced motion и accessibility;
- TypeScript, ESLint и production build прошли;
- встроенный браузер подтвердил остановку hero вне viewport, сохранённый поворот чата и нулевой горизонтальный overflow;
- три независимых финальных критика проверили визуал, доступность и технический жизненный цикл; их замечания по offscreen-паузе и transform-изоляции устранены.
- commit `b7afacb` опубликован как атомарный release `/var/www/kaigo-marketing/releases/b7afacb`;
- production smoke подтвердил `200` для лендинга, `401` для защищённых `/studio` и `/builder/`, а также сохранность публичных demo-маршрутов;
- серверный Node обновлён с 18.19.1 до официального Node 22.23.1 с проверкой SHA-256, потому что Vite 8 требует Node 20.19+.
- после переноса результатов удалены 23 завершённых сабагента этой motion-задачи; освобождено 573 439 092 байта, родительская задача и остальные исследовательские агенты сохранены.

Доказательства:

- `docs/evidence/2026-07-27-expressive-motion/hero-complete-1920.png`;
- `docs/evidence/2026-07-27-expressive-motion/landing-full-1920.png`;
- `docs/evidence/2026-07-27-expressive-motion/landing-full-390.png`;
- `docs/evidence/2026-07-27-expressive-motion/README.md`.

Потенциал для публикации:

- Пост «Как мы превратили тусклую линию в понятную историю появления AI-виджета» с кадрами source, scan и complete.
- Пост «Почему красивую анимацию надо уметь выключать» о viewport-паузе, reduced motion и тестах реального браузера.

## 2026-07-27 — Compact desktop, K-mark, favicon и title проверены

Статус: опубликовано на production и проверено

Подтверждённые факты:

- При viewport `1536×830`, соответствующем рабочей области Windows с масштабом
  125%, первый экран полностью помещается без просьбы уменьшать масштаб:
  header — 94 px, H1 — 48.384 px, цель логотипа — 44 px, горизонтальный
  overflow — 0.
- Тот же compact-контракт повторно проходит при `1366×768`: header — 94 px,
  H1 — 46 px, цель логотипа — 44 px, горизонтальный overflow — 0.
- Новый геометрический знак `K`, favicon `/favicon.svg` и title
  `Kaigo — AI в вашем бизнесе за 10 минут` входят в unit- и E2E-контракты.
- Проверка завершилась результатами 93/93 unit-теста, 12/12 Playwright-сценариев,
  успешными lint и production build; desktop 1920, mobile 390, reduced motion и
  accessibility сохранились.
- Frontend опубликован как неизменяемый release `f734338`; GitHub, серверный
  checkout и nginx-маршрут favicon обновлены до `7f5432a`.
- Production smoke подтвердил `200` для лендинга, favicon, hashed JS, API health
  и публичного виджета, а также ожидаемые `401` для Studio и Builder.
- Найденная после первого deploy ошибка `/favicon.svg` → `404` оказалась не
  ошибкой сборки: файл лежал в release, но уходил в fallback старого приложения.
  Exact nginx-route с `no-cache` закреплён отдельными deployment-тестами.
- Полный пакет чисел, команд и ссылок:
  `docs/evidence/2026-07-27-compact-desktop/README.md`.

Ограничение:

- Финальные in-app screenshots не сохранены: браузер был временно отключён во
  время последней попытки. Неподтверждённые или пустые PNG не создавались.

План:

- После восстановления in-app Browser отдельно сохранить кадры `1536×830` и
  `1920×1080`.

## 2026-07-28 — GLM-5.2 и GPT-5.5 собрали один виджет по одинаковому контракту

Статус: локальный сравнительный прогон завершён и проверен

Подтверждённые факты:

- Обе модели получили одинаковый brief Flowwow, grounded-контекст, контракт
  `chat-v1`, температуру `0.9` и до двух автоматических ремонтов.
- Каждый вариант целиком создавался одной моделью: без смешивания GLM и GPT
  внутри генерации или ремонта.
- GLM-5.2 исправил 12 серверных ошибок за один repair-вызов; итог — 0 ошибок,
  511,637 секунды, 60 593 input и 126 635 output tokens, расчётно 112,34 ₽.
- GPT-5.5 исправил 4 ошибки за один repair-вызов; итог — 0 ошибок,
  542,154 секунды, 39 537 input и 15 196 output tokens, расчётно 38,31 ₽.
- Оба preview вручную проверены во встроенном браузере: открытие, закрытие,
  ввод и отправка сообщения работают, появляется пользовательский ход и
  pending-состояние ответа.
- В GLM-варианте обнаружен дополнительный browser-level пробел: исходный
  launcher сделан `div`, хотя визуально и через runtime он работает. GPT сразу
  вернул семантическую кнопку.

Доказательства:

- `benchmarks/agentrouter/results/flowwow-pure-model-v1/README.md`;
- финальные и промежуточные JSON/HTML, usage, request IDs и validation issues в
  каталогах `glm-5.2/` и `gpt-5.5/`.

Потенциал для публикации:

- Пост «Дороже модель — не значит дороже результат»: GPT-5.5 имел более высокую
  цену за миллион, но в этом прогоне потратил почти в 2,9 раза меньше денег за
  счёт гораздо меньшего output/reasoning.

## 2026-07-28 — Gemini подключён к общему маршрутизатору моделей

Статус: локальная интеграция завершена и проверена

Подтверждённые факты:

- Новый адаптер официального `google-genai` принимает обычный текст,
  структурированный JSON и бинарные изображения через единый модельный контракт.
- Туннельный `base_url` и версия Gemini API сохраняются; глобальный лимит
  выходных токенов не добавляется ни в запрос, ни в конфигурацию провайдера.
- Ошибки квоты, отсутствующей модели, таймаута и общей недоступности приводятся
  к типам, которые уже понимает fallback и audit общего маршрутизатора.
- Для биллинга Gemini output включает candidate и thinking ровно один раз;
  thinking остаётся отдельной диагностической частью output.
- Старый direct-движок сохранил API, retry-политику и прежний расчёт
  `TokenUsage`, но теперь использует общие разбор usage, ошибок и `base_url`.
- Маршрутизатор проверяет возможности провайдера до вызова: AgentRouter не
  получает бинарные изображения, неподдерживаемая попытка видна в audit, а
  запрос переходит к image-capable fallback.
- Structured-запрос AgentRouter принимает только цельный JSON, проверенный по
  локальной JSON Schema; prose, Markdown fences, пропущенные и лишние поля не
  считаются успешным ответом.
- Даже отклонённый после ответа результат сохраняет usage и request ID в audit,
  поэтому fallback не скрывает уже оплаченные токены и стоимость первой попытки.
- Qwen Code закреплён на проверенной версии `0.21.0`, получает только
  allowlisted host environment и запускается в отдельной process group/session;
  timeout и cancellation завершают всё дерево процессов.
- Routed и legacy direct Gemini используют одну адаптацию builder-схем для
  различий Gemini 2.5, 3.5 и 3.6.
- Устаревший output cap удалён из core runtime, админки, маршрутов виджета и
  пресетов; timeout, retry и отдельный safety limit Antigravity по общему
  расходу сохранены.
- Расширенный целевой набор проверок завершился результатом 71 passed и одного
  пропущенного environment-dependent packaging-теста.

## 2026-07-28 — Project API и надёжное завершение trial

Статус: локальная реализация завершена и проверена

Подтверждённые факты:

- Добавлен аутентифицированный Project API: создание durable run, чтение проектов,
  запусков, событий, preview и artifacts; запуск не вызывает generation engine inline.
- Создание run атомарно резервирует trial, пишет `run.created` и защищено
  `Idempotency-Key`, CSRF, tenant/owner checks и блокировкой проекта.
- Worker записывает структурированную категорию ошибки и идемпотентно завершает
  trial по terminal run; recovery восстанавливает пропущенный settlement и ledger
  model calls после сбоя процесса.
- SSE поддерживает replay через `Last-Event-ID`, скрывает внутренние payload-поля,
  не держит транзакцию во время ожидания и закрывается после terminal event.
- После review учёт model call сохраняет durable dispatch до вызова провайдера:
  отмена после dispatch оставляет ровно одну audit-запись и расходует trial.
- Recovery больше не блокируется историческими non-trial run, а preview и
  компенсация признают только структурно валидный принятый artifact или draft.
- Trial recovery помечает повреждённые связи reservation/entitlement как
  `quarantined`, поэтому они не блокируют более новые корректные settlement;
  временные ошибки базы остаются в очереди для повторной попытки.
- Terminal SSE выгружает весь backlog страницами, а shutdown не начинает новый
  claim после остановки во время settlement recovery.
- Проверки после hardening: 251 passed / 7 skipped в основном bounded-наборе,
  ещё 133 passed в независимом builder-наборе и 4 passed на PostgreSQL 15;
  отдельно проверен реальный Alembic backfill с `0006` на `0007`.

## 2026-07-28 — Неизменяемые публикации и стабильный embed

Статус: локальная реализация завершена и проверена

Подтверждённые факты:

- Публикация создаёт неизменяемый release из принятого artifact и сохраняет один стабильный embed-key на проект; повторная публикация того же artifact идемпотентна.
- Rollback атомарно переключает active release, а checksum и принадлежность release перепроверяются до активации.
- Публикация и rollback в одной транзакции повторно проверяют владельца, tenant, подтверждённую OAuth identity и активную подписку; PostgreSQL-тест воспроизводит конкурентную отмену подписки.
- Runtime не публикует prompt, source URL, provenance или model traces. Принятые HTML/CSS отображаются во внутреннем sandbox iframe, но сгенерированный JavaScript в публичной версии не исполняется; открытие, закрытие, чат и геометрия принадлежат trusted runtime Kaigo.
- Публичный visitor chat использует тот же bounded model router, короткоживущий HMAC-capability, allowlist origin, независимые лимиты публикации/IP/сессии и доверенную обработку proxy-chain. Остаточный риск scraping публичного виджета и process-local природа лимитера зафиксированы отдельно.
- Chromium-тест подтвердил двухходовый разговор, закрытый hitbox и то, что `document.body.replaceChildren()`/`location.replace` не стирают trusted controls и не создают внешний запрос.
## 2026-07-28 — Основа подписочного биллинга YooKassa

Статус: локальная реализация, без реального списания и production deploy

- Добавлен provider-neutral платёжный контракт и YooKassa adapter с обязательной
  серверной перепроверкой каждого webhook через API провайдера.
- Checkout использует серверный каталог тарифов, per-user idempotency и не держит
  транзакцию базы во время внешнего HTTP-запроса.
- Подтверждённый платёж в одной транзакции создаёт/продлевает подписку и пишет
  неизменяемые webhook/usage записи; redirect успеха не выдаёт доступ.
- Studio подключена к checkout и bounded polling статуса без хранения платёжных
  секретов в браузере.
- Зафиксирована политика: уже опубликованный виджет не выключается молча после
  окончания подписки, но публикация новой версии требует активный тариф.
- Повтор checkout теперь привязан к неизменяемому fingerprint аккаунта магазина.
  После потерянного ответа смена `shop_id` останавливает recovery до нового
  запроса в YooKassa, поэтому одна попытка не создаёт платежи у двух магазинов.
- Зафиксирована безопасная drain-политика смены магазина: пока существует
  незавершённая попытка старого merchant-аккаунта, новый checkout запрещён,
  pending API скрывает платёжную ссылку, а webhook остаётся повторяемым через
  временный `503` до восстановления прежней конфигурации.

## 2026-07-28 — Публичный SaaS-контур и безопасный production foundation

Статус: локальная реализация и проверки завершены; production deploy ещё не выполнялся.

- Лендинг и Studio теперь ведут в owner-scoped SaaS flow: явная ссылка всегда создаёт новый
  проект, OAuth возвращает к нужному проекту, а `/studio` больше не включает legacy `/builder`.
- Первый результат описан честно: одна бесплатная express-версия после входа, без карты;
  обычная оценка времени — 10–20 минут, сложные сайты могут обрабатываться дольше.
- Добавлена privacy-safe воронка из server-owned событий без URL, brief, prompt, email,
  профиля и IP; повторные запросы и webhooks не создают дубликаты.
- Production startup больше не создаёт схему через ORM. Добавлен fail-closed preflight для
  точной legacy-схемы и проверенный Alembic-переход до ревизии `0010_funnel_events`.
- Worker вынесен под systemd с обязательным egress guard; контейнеры и nginx получили
  отдельные production-контракты для DB, API/SSE, Studio, billing и public runtime.
- Самодостаточный smoke проходит реальный HTTP/OAuth/session/project/run/worker/trial/
  billing/publication/rollback путь с тестовыми провайдерами без внешних API.

## 2026-07-29 — Честные публичные обещания MVP

Статус: локальная правка публичного интерфейса и визуальная проверка завершены.

- Лендинг, FAQ и экран входа больше не обещают доработку проекта, редактирование
  ответов или историю версий: доступны предпросмотр, проверка диалога, публикация
  и подключение готового виджета.
- Публичная демонстрация Studio сохранила двухколоночную композицию, но
  интерактивная имитация недоступной доработки заменена статическим списком
  реальных шагов.
- Обновлённые desktop- и mobile-снимки проверены визуально; focused unit и
  Playwright-сценарии проходят.

## 2026-07-29 — Гибридный AI-маршрут, устойчивый OAuth и компактный desktop

Статус: реализация и локальные проверки завершены; реальный вход через OAuth
ожидает клиентские данные провайдера.

- Согласованный заголовок для бизнеса возвращён на первый экран: «Через 10 минут
  вы сможете сказать: наш бизнес использует AI». Рядом явно описана бесплатная
  первая версия персонального AI-виджета.
- Для экранов с эффективной шириной 1281–1680 px уменьшены заголовок, шапка и
  карточки анимации. Полный цикл проверен без наложений при 1366×768, 1536×960,
  1680×960 и без регрессии на 1920×1080.
- OAuth больше не падает из-за удалённого черновика или неожиданной ошибки
  Google/Яндекса: состояние входа остаётся одноразовым и привязанным к браузеру,
  а пользователь получает безопасное русское сообщение и может повторить вход.
- Добавлен production-маршрут моделей: GPT-5.5 предлагает направления, выбирает
  концепцию, планирует композицию и судит результат; GLM-5.2 пишет и исправляет
  HTML/CSS/JavaScript. Анализ исходного сайта и критика скриншотов пока остаются
  на мультимодальном Gemini.
- Worker содержит заранее установленный Qwen Code 0.21.0 и не скачивает пакеты во
  время выполнения. Без ключа, HTTPS endpoint или исполняемого клиента гибридный
  режим останавливается до платного вызова.
- Приватный аудит сравнений теперь хранит роль, модель, usage, request ID и hash
  промпта, но не сохраняет сам prompt, текст исключения или секреты. Каждая
  попытка получает отдельную неизменяемую квитанцию.
