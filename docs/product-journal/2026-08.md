# Kaigo product journal — август 2026

## 1 августа — контрольная production-генерация на новом сайте

Статус: завершено и проверено.

- Создан изолированный технический пользователь и проект для `simplecoffee.ru`.
- Первый запуск остановился на временной недоступности провайдера; trial был
  компенсирован автоматически, повтор разрешён штатным API.
- Повторный запуск завершился на 100%, сохранил ревизию 5 и прошёл визуальный gate.
- Визуальный judge contract исправлен: судья получает только eligible-находки уровня
  blocker/major с confidence не ниже 0,75; quorum остаётся fail-closed.
- Сохранены шесть forensic-кадров desktop/mobile и опубликовано замороженное превью:
  <https://kaigo.space/builder-comparison/agent-kernel-v1/simplecoffee-control-v1/>.
- Реальный owner-scoped chat ответил по найденным акциям и контактам сайта.
- Генерация заняла 10 минут 27,6 секунды, использовала 438 087 токенов с thinking и
  стоила $0,437133.

Следующий продуктовый фокус: кэш успешных критиков, повтор только сбойной роли,
сокращение repair на арт-направлении и grounding стартовых сообщений.

## 1 августа — визуальные критики больше не повторяют уже завершённую работу

Статус: реализовано и проверено тестами.

- При повторной проверке тех же кадров Kaigo повторно вызывает только критика, который не ответил или вернул ошибку.
- Два успешных ответа сохраняются внутри текущего visual gate; независимый судья по-прежнему запускается заново.
- Любой новый браузерный аудит полностью сбрасывает сохранённые ответы, поэтому проверка после ремонта остаётся независимой.
- В тестовом сценарии расход prompt-токенов повторной попытки снизился на 43,4% — с 53 до 30.
- По телеметрии Simple Coffee такой механизм исключил бы повторные вызовы примерно на `$0.049732` и до 28,8 секунды ожидания. Это расчёт для следующего контрольного прогона, а не уже измеренная production-экономия.

Доказательства: [`2026-08-01-visual-critic-reuse.md`](../release-evidence/2026-08-01-visual-critic-reuse.md).

## 1 августа — полный production-прогон Dodo Brands

Статус: завершено, опубликовано и проверено.

- Отдельный подтверждённый тестовый пользователь прошёл реальный SaaS-путь для
  `dodobrands.io`; run завершился на 100% и сохранил пять verified-ревизий.
- Временный сбой основного Gemini-маршрута теперь получает вторую route-попытку.
- Prompt генератора использует тот же точный allowlist HTML, что и
  детерминированный валидатор; прежнее падение на `forbidden_element` и
  `forbidden_attribute` устранено.
- Три визуальных критика, судья и browser gate приняли результат с первой
  визуальной попытки; owner-scoped чат вернул HTTP 200.
- Итог: 13:55, 126 484 входных и 94 317 выходных токенов, `$0.273738`.
- Публичное превью:
  <https://kaigo.space/builder-comparison/agent-kernel-v1/dodobrands-control-v1/>.

Доказательства: [`2026-08-01-dodobrands-control-live-run.md`](../release-evidence/2026-08-01-dodobrands-control-live-run.md).

## 1 августа — гибридный worker и диагностика блокировки Gemini

Статус: инфраструктура обновлена, новый полный прогон заблокирован внешним биллингом.

- Production worker переключён на GPT-5.5 + GLM-5.2; Gemini оставлен для операций,
  которым нужны изображения.
- Visual committee сохраняет quorum 2/3 и теперь переживает только временную потерю
  всех маршрутов: до трёх попыток с паузами 5 и 15 секунд.
- Создан отдельный production-тестовый пользователь без публичного auth bypass.
- Два контрольных запуска Simple Coffee дошли до снятия сайта, но завершились до
  расхода токенов: официальный Gemini endpoint вернул `403 PERMISSION_DENIED` из-за
  billing/dunning deny Google-проекта.
- Бесплатная попытка тестового пользователя восстановлена.
- Ранее успешное публичное превью проверено во встроенном браузере на 1920×1080 и
  390×844: горизонтального overflow нет, панель остаётся компактной.
- Выявлен UX-дефект: при недоступном chat provider публичный виджет слишком долго
  показывает «Gemini готовит ответ» без понятного terminal-сообщения.

Следующий обязательный шаг — заменить или разблокировать vision-маршрут, затем
повторить полный гибридный прогон и отдельно добавить bounded timeout/fallback для
visitor chat.

## 1 августа — точная диагностика Gemini и безопасное публичное превью

Статус: исправление развёрнуто и проверено в production.

- Ошибка `403 PERMISSION_DENIED` теперь сохраняется как отдельный terminal-код
  `provider_permission_denied`, не маскируется под исчерпание маршрутов и не запускает
  бессмысленные повторы.
- Публичное сообщение не содержит внутреннюю диагностику провайдера: пользователь
  видит понятное объяснение ограничения доступа или оплаты.
- Замороженное публичное превью больше не зависает на «Gemini готовит ответ»:
  top-level демонстрация сразу объясняет, что живой диалог доступен в Studio, и
  возвращает форму в рабочее состояние.
- Отдельный тестовый пользователь создан непосредственно в production-БД. Контрольный
  запуск на `rawbureau.ru` завершился за 4 минуты 12 секунд точной ошибкой доступа на
  этапе анализа референса; модель не получила prompt, расход составил 0 токенов и
  $0, бесплатная попытка автоматически восстановлена.
- Более тяжёлый `dodobrands.io` показал отдельную проблему производительности:
  визуальный обход сайта занял больше шести минут до первого вызова модели. Запуск
  был безопасно отменён, trial также восстановлен.
- Четыре завершённые дочерние задачи Codex удалены из локального хранилища; освобождено
  104,57 MiB.

Следующий технический фокус — ограничить и ускорить визуальный обход сайта, затем
добавить мультимодальный fallback, чтобы reference analysis не зависел только от
одного Gemini-проекта.

## 1 августа — визуальный обход референса ускорен и развёрнут

Статус: реализовано, проверено независимыми ревью и развёрнуто в production.

- Двухпроходный desktop/mobile capture сохранён, включая кадры сверху, из середины и снизу страницы.
- Повторные последовательные ожидания удалены, reverse reset получил один финальный settle, а таймаут шрифтов стал явным non-fatal предупреждением.
- Lazy-load fixture ускорился с 65,565 до 34,441 секунды, примерно на 47,5%.
- Production crawl `python.org` успешно завершился с complete coverage за 74,66 секунды, без вызова модели, токенов и стоимости.
- `kontur.ru` выявил следующий отдельный узкий участок: защищённая навигация может дважды исчерпать page deadline ещё до scroll capture.
- Production commit: `bcbbf73`; HTTP smoke и лендинг проверены во встроенном браузере Codex.

Доказательства: [`2026-08-01-adaptive-reference-capture.md`](../release-evidence/2026-08-01-adaptive-reference-capture.md).

## 5 августа — Studio перестроена в одноэкранное AI-пространство

Статус: реализовано, проверено и открыто в изолированном preview.

- Слева собраны чат с Kaigo, понятные этапы, меняющийся рабочий статус и поле
  следующей доработки; справа расположен большой предпросмотр виджета.
- Контекст сайта сворачивается, а проекты, история версий и публикация открываются
  в боковых панелях поверх рабочего экрана.
- После пользовательской проверки левая рабочая зона расширена примерно до 44%
  экрана, шрифты сообщений, статусов и основные кнопки увеличены; предпросмотр
  стал компактнее и сохраняет достаточно места для реального виджета.
- Версия и статус проверки собраны в одну строку с предпросмотром, зелёные
  рабочие акценты заменены на оранжевую палитру Kaigo.
- Добавлена отдельная боковая панель «Тариф и лимиты» с планом, остатком токенов,
  датой продления и переходом к управлению публикацией.
- На мобильном экране добавлены отдельные вкладки «Чат» и «Предпросмотр».
- Прошли 219 frontend-тестов, 14 browser-сценариев, ESLint, TypeScript и
  production-сборка; accessibility-проверка не нашла серьёзных или критических
  нарушений.
- Изолированный preview обновлён из ветки `codex/product-ui`; страница и API отвечают HTTP 200,
  на 390 × 844 нет прокрутки документа и ошибок в консоли.
- Production checkout, nginx, production-сервисы и backend не изменялись.

Доказательства: [`2026-08-05-friendly-studio`](../release-evidence/2026-08-05-friendly-studio/README.md).

## 6 августа — лендинг объясняет путь от сайта до AI-консультанта

Статус: реализовано и проверено в ветке `codex/product-ui`.

- Первый экран получил прямое обещание результата и отдельное пояснение:
  посмотреть и проверить первую версию можно бесплатно, оплата нужна только
  перед публикацией.
- Условная маленькая плашка заменена переиспользуемым вертикальным
  AI-виджетом со статусом, приветствием, примером вопроса, полем ввода и
  объяснением источника ответов.
- Повторяющийся макет дома сохранён только в архитектурном сценарии; добавлены
  отдельные визуальные истории пекарни и керамической мастерской.
- «Как это работает» перестроено в цельный процесс из трёх действий и
  безопасного пути публикации внутри одной панели. Анимация больше не проходит
  поверх контента; экран занимает 1079,5 px при viewport 1920 × 1080.
- Блок бесплатного старта собран как одна металлическая поверхность с крупным
  обещанием и тремя условиями вместо набора одинаковых белых карточек.
- На макетах появился адрес сайта и постоянная подпись «Ваш сайт». Кейс
  «до/после» стал контрастнее, получил встроенный вертикальный виджет и
  занимает 1079,9 px при viewport 1920 × 1080.
- Лендинг приведён к системе «тёплый белый — металлический серый — оранжевый»;
  усилены типографика, контраст, состояния кнопок и ненавязчивые фоновые узоры.
  `reduced motion` сохранён.
- Прошли 232 frontend-теста, 14 browser-сценариев, ESLint, TypeScript,
  production-сборка и axe accessibility без серьёзных или критических
  нарушений.
- Проверены desktop 1920 × 1080, 1680 × 960, 1536 × 830, 1366 × 768 и mobile
  390 × 844; вертикальная форма виджета и отсутствие горизонтального
  переполнения проверяются браузерными контрактами.
- Production checkout, nginx, production-сервисы и backend не изменялись.

Материалы: [`2026-08-06-clear-business-landing`](../telegram/assets/2026-08-06-clear-business-landing/README.md).

## 8 августа — путь до готового AI-сотрудника показан в трёх этапах

Статус: реализовано и проверено в ветке `codex/product-ui`.

- Полноэкранный product tour сокращён до трёх этапов: ссылка и пожелание,
  бесплатная проверка результата в Studio, публикация готового виджета на
  сайте. Сборка и проверка больше не разнесены по разным экранам.
- Во всех этапах используется современный цифровой B2B-сайт NovaFlow вместо
  пекарни. Для него сгенерировано металлическое цифровое ядро с прозрачным
  фоном.
- Слева добавлены короткие инструкции обычным языком; заголовки уменьшены и
  реже переносятся. На втором этапе видна сама Studio и один крупный виджет без
  сайта на фоне и без мобильного дубля.
- Бесплатно можно проверить первую версию и поговорить с виджетом; пожелание
  сохраняется в Studio, а доработки доступны после выбора тарифа.
- Этапы автоматически переключаются каждые 8 секунд по кругу. Пользователь
  может листать их стрелками, выбирать напрямую и ставить автолистание на
  паузу; при `reduced motion` оно не запускается.
- Кнопка отдельной демонстрации удалена из лендинга, но прямой маршрут `/tour`
  сохранён. На 1920 × 1080, 768 × 1024 и 390 × 844 каждый этап вместе с
  навигацией помещается в один viewport без горизонтального overflow.
- Прозрачные изображения цифрового объекта оптимизированы до WebP: 36,7 КБ
  для основного кадра и 5,3 КБ для аватара; включена ленивая загрузка.
- Прошли 241 frontend-тест, 17 browser-сценариев, ESLint, TypeScript,
  production-сборка и axe accessibility без серьёзных или критических
  нарушений.
- Production checkout, nginx, production-сервисы и backend не изменялись.

Материалы: [`2026-08-08-visual-product-tour`](../telegram/assets/2026-08-08-visual-product-tour/README.md).

## 11 августа — семь этапов интерфейса открыты на одном домене

Статус: архив развёрнут и проверен на `kaigo.space`.

- На <https://kaigo.space/frontend/> появилась единая хронология из семи
  визуально значимых версий: от первой friendly Studio до финального
  product tour из трёх этапов.
- Каждая версия собрана из точного commit SHA и открывается по постоянному
  адресу `/frontend/vN/`. Для Studio и product tour добавлены отдельные прямые
  ссылки, которые сразу показывают нужный экран, а не промежуточный список.
- Архивные приложения используют отдельный preview API. Корневые API-ссылки,
  iframe и переходы переписаны в служебный namespace; production cookies,
  authorization headers и ответы `Set-Cookie` туда не передаются.
- Текущие `/` и `/studio` после подключения остались без изменений: HTTP 200,
  одинаковый размер 930 байт и тот же SHA-256
  `f42547eb225076bd65de5c41a5117af448c5496e98deea164565a744c5f2ea3d`.
- Все публичные страницы архива отвечают HTTP 200, `/frontend` перенаправляет
  на `/frontend/`. В браузере проверены прямые Studio, лендинги и оба tour;
  сломанных изображений, предупреждений и ошибок в консоли нет.
- На 390 × 844 индекс, Studio v3 и tour v7 не имеют горизонтального
  переполнения. Автолистание tour v7 подтверждено переходом с третьего этапа
  на первый.
- В GitHub отправлены commits `386f702` и `43ef9d0`; активирован неизменяемый
  серверный release `20260811-ui-history-43ef9d0`.

Материалы: [`2026-08-11-frontend-history`](../telegram/assets/2026-08-11-frontend-history/README.md).

## 13 августа — лендинг показывает реальный путь FORMA

Статус: реализовано и проверено в ветке `codex/product-ui`.

- На первом экране восстановлено обещание «Через 10 минут вы сможете сказать:
  наш бизнес использует AI». Форма, пояснение бесплатного старта и текущая
  анимация сохранены.
- Второй экран теперь использует реальный кейс FORMA и три понятных этапа:
  ссылка и пожелание, проверка готового виджета в Studio, подключение одной
  строкой кода. В кадрах показаны настоящий сайт и реальный ответ виджета.
- Тёмный самостоятельный визуальный язык product tour заменён общей системой
  лендинга: тёплый белый, холодный металлический серый и оранжевый акцент.
  Этапы автоматически переключаются раз в восемь секунд, но отдельной кнопки
  автолистания больше нет.
- Дублирующий блок «Как это работает» скрыт из основного потока. Инструкции по
  Tilda и обычному HTML перенесены в третий этап.
- В блоках визуального анализа, кейса «до/после» и финального действия
  переработаны иерархия текста, переносы и адаптивная компоновка. Описания
  состояний кейса перенесены под изображения; финальная форма не перекрывает
  поле или кнопку на промежуточных ширинах.
- Подготовлены три направления знака Kaigo: AI Portal, Living Layer и Answer
  Signal. Они вынесены на отдельную сравнительную страницу; текущий логотип не
  заменён без выбора направления.
- Прошли 19 frontend-тестов, TypeScript, ESLint, production-сборка, 16
  browser-сценариев desktop/mobile/reduced-motion/accessibility и отдельная
  проверка восьмисекундного автоперехода `1 → 2`.
- Production checkout, nginx, production-сервисы и backend не изменялись.

Материалы: [`2026-08-13-forma-landing`](../telegram/assets/2026-08-13-forma-landing/README.md).
