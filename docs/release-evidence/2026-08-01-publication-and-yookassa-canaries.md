# Контрольные проверки публикации и ЮKassa — 1 августа 2026

## Внешняя HTTPS-публикация

Проверка выполнена на отдельном публичном домене, который имитирует сайт клиента:

- разрешённый origin: `https://canary.5-129-236-90.sslip.io`;
- запрещённый origin: `https://denied-canary.5-129-236-90.sslip.io`;
- приложение: `https://kaigo.space`;
- проект: `18527347-26a1-5e57-bae4-4f36fc0b6a93`;
- стабильный публичный ключ: `b9y2y5-96C17aDWu4cTqlo30sUWrzjsM`.

Контрольный прогон завершён со статусом `passed` за 5,99 секунды. Подтверждено:

- загрузка опубликованного виджета на разрешённом HTTPS-домене;
- открытие launcher и ответ чата;
- переключение с базовой версии на новую;
- откат на базовую версию;
- запрет рабочего виджета на неразрешённом домене;
- точный `frame-ancestors` в CSP;
- отсутствие cookie и иных credential-заголовков в публичных запросах;
- отсутствие данных виджета в публичных browser storage.

Все API-операции публикации, переключения версии и отката вернули HTTP 200. Полное обезличенное доказательство хранится на сервере в `/srv/kaigo/releases/publication-canary-sslip-latest.json` с правами `0600`.

Во время проверки был найден дефект самого canary: release marker находится в скрытом элементе `[data-kaigo-release][data-kaigo-release-id]`, а проверка искала его на `body`. Добавлен реальный Playwright-регрессионный тест. После исправления весь набор `test_publication_https_canary.py` проходит: `14 passed`.

## Тестовый checkout ЮKassa

Проверка выполнена на production-контуре Kaigo с тестовым магазином ЮKassa. Боевые карты и реальные списания не использовались.

Подтверждено:

- сервер создал checkout тарифа `starter_monthly` через публичный API Kaigo;
- сумма формируется серверным каталогом: `199000` копеек, то есть 1 990 ₽;
- валюта — RUB;
- получена HTTPS-страница оплаты на домене `yoomoney.ru`;
- статус в Kaigo и у ЮKassa — `pending`;
- ЮKassa явно вернула `test_mode=true` и `paid=false`;
- metadata платёжного объекта привязана к UUID попытки оплаты Kaigo;
- повтор запроса с тем же `Idempotency-Key` вернул тот же UUID и `created=false`, поэтому дубликат платежа не появился;
- локальный регрессионный набор provider/service/routes/receipts/renewals проходит: `123 passed`.

Обезличенные доказательства сохранены на сервере:

- `/srv/kaigo/releases/yookassa-test-checkout-latest.json`;
- `/srv/kaigo/releases/yookassa-provider-verification-latest.json`.

Тестовый платёж намеренно оставлен в `pending`: пользовательская карта не вводилась. Полную активацию подписки, сохранение `payment_method_id` и последующее тестовое автопродление нужно проверять отдельным ручным сценарием с официальной тестовой картой ЮKassa.

## Следующий контрольный прогон

После входа через Яндекс во встроенном браузере нужно выполнить одну полную генерацию для `https://rawbureau.ru/`, сохранить run ID, длительность, токены, model lineage, этапы исправлений, desktop/mobile-кадры и проверить реальный диалог с опубликованным виджетом. Эти данные станут базовой линией перед оптимизацией скорости и стоимости.
